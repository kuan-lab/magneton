# Session Log — April 24, 2026

## Summary
Wired SLURM `--qos` support into the pytc HPC submitter, ran end-to-end test inference + instance segmentation for both bouton (65k) and synapse (110k) checkpoints on a held-out fib_b chunk, and updated the `/config` skill registry to cover the new pytc training/inference conventions.

## Changes Made

**Modified: `pytorch_connectomics/tools/run_hpc.py`**
- Read optional `hpc.qos` from the stage HPC config and emit `#SBATCH --qos=<value>` after `--partition`/`--constraint` (uncommented prior placeholder)

**Modified: `.claude/skills/config/SKILL.md`**
- pytc training row now lists `synapse training`/`mito training`/`bouton training` keywords and references `Isotropic-Synapse-Base.yaml` (160³), `Isotropic-Bouton-Base.yaml` (192³), `Lucchi-Mitochondria.yaml` (112³) alongside the existing neuron template
- pytc inference row extended to cover `pre-train-hpc` and lists `hpc_train_synapse.yaml`/`hpc_train_bouton.yaml` as training-profile examples
- New gotcha: single self-contained yaml + `blank.yaml` placeholder pattern, with the merge-order rule (full yaml at `config_base`, blank at `config_file`)
- New gotcha: synapse vs mito input-size scaling rationale (1.28 μm³ context for cleft detection)
- New gotcha: `affinity_prediction.hpc` is loaded for training too — a training yaml needs `time: "24:00:00"` + `mutil_jobs: false`, distinct from inference yamls
