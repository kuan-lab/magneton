# Session Log — June 02, 2026

## Summary
Plumbed `INFERENCE.GEOMETRY.MIP` through the direct-precomputed inference path so a model can read from a downsampled scale of the input pyramid while the output is written as a fresh single-scale precomputed at mip=0; added a `/globus` skill that wraps the Globus CLI for natural-language dataset transfers.

## Changes Made

**New File: `.claude/skills/globus/SKILL.md`**
- `/globus <english>` wrapper around `globus transfer`: parses the argument to resolve source dataset path (default endpoint = Yale CRC Misha) and destination path (default = KuanLab nginx at `/nginx_share/marmoset/`)
- Endpoint UUIDs hardcoded (Misha source, KuanLab dest); auth tokens via gpfs-shared `~/.globus/cli/` so radev can submit using Misha login
- Confirms src→dst with the user via `AskUserQuestion` before submitting; supports endpoint/path overrides in the English (e.g. "from radev to misha", "to my home")

**Modified: `pytorch_connectomics/connectomics/config/defaults.py`**
- Added `_C.INFERENCE.GEOMETRY.MIP = 0` with a comment explaining when to bump it (e.g. an 8nm-trained model reading mip=1 of a 4nm-base precomputed)

**Modified: `pytorch_connectomics/tools/run.py`**
- `_run_precomputed` reads `geom.MIP`, passes it to `volume_info_from_cv`, `init_output_volume`, and the input `CloudVolume(..., mip=mip)`; output still opened at mip=0 since it's the freshly created single-scale precomputed

**Modified: `pytorch_connectomics/tools/run_hpc.py`**
- `_slurm_script` pulls `_mip` from the geometry block and uses it for the pre-create `volume_info_from_cv` and `init_output_volume` calls so the output's scales[0] matches the chosen input scale

**Modified: `pytorch_connectomics/init_prec_output.py`**
- `init_output_volume` always opens the output URL at `mip=0` (was reusing the input `mip` arg) — the output is single-scale, so the read mip is fixed at 0 regardless of the input scale selected
