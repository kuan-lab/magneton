# Session Log — March 30, 2026

## Summary
Optimized PyTC inference configuration for isotropic FIB-SEM data, enabled TF32 compute, and created new isotropic neuron training config with seed-based ground truth labels.

## Changes Made

**Modified: `/gpfs/radev/home/yf354/pytorch_connectomics/connectomics/engine/trainer.py`**
- Enabled TF32 compute: `allow_tf32 = False` → `True` for both `cuda.matmul` and `cudnn`, giving ~2x throughput on A100/H100 with negligible accuracy impact

**New File: `pytorch_connectomics/configs/Isotropic-Neuron-Base.yaml`**
- New training/inference config for isotropic FIB-SEM neuron segmentation
- `DATASET.IS_ISOTROPIC: true` — all U-Net levels use `(3,3,3)` kernels instead of anisotropic `(1,3,3)` defaults
- Isotropic input/output size `[128, 128, 128]` (was `[17, 257, 257]`)
- `LABEL_EROSION: None` — disabled, since training labels already have gaps between segments. Set to `1` if ground truth labels have touching (gap-free) segment boundaries
- `INFERENCE.SAMPLES_PER_BATCH: 16` (was 4), `SYSTEM.NUM_CPUS: 8`
- Isotropic inference stride `[64, 64, 64]` and pad `[32, 32, 32]`
- `ITERATION_TOTAL: 500000`
- Training data: 3 volumes via txt file lists (fib_b, fib_c, fennel)
- Output path: `outputs/Marmoset/neuron/isotropic_v1`

**New File: `pytorch_connectomics/configs/Isotropic-Neuron-Affinity-UNet.yaml`**
- Architecture overlay specifying `unet_3d` with `residual_se` blocks
